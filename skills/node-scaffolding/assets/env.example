# Runtime secrets and overrides — never commit this file.
# Copy to .env and fill in your values.

# <PROJECT_NAME>_DATABASE__URL is only needed when the database module is enabled (withDb=yes).
<PROJECT_NAME>_DATABASE__URL=postgres://user:pass@localhost:5432/mydb
